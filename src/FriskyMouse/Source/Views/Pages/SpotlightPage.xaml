<Page
    x:Class="FriskyMouse.Views.Pages.SpotlightPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:controls="clr-namespace:FriskyMouse.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FriskyMouse.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Mouse Highlighter Settings"
    d:DataContext="{d:DesignInstance local:SpotlightPage,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="1500"
    d:DesignWidth="1000"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <StackPanel Margin="0,20,15,0" Orientation="Vertical">
        <controls:SettingsCard
            Description="Turn on or off the mouse highlighter feature"
            Header="Mouse highligther status"
            Icon="{ui:SymbolIcon DesktopCursor28}">
            <ui:ToggleSwitch
                IsChecked="{Binding ViewModel.IsMouseSpotlightEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                OffContent="Off"
                OnContent="On" />
        </controls:SettingsCard>
        <ui:TextBlock
            Margin="0,20,0,0"
            FontTypography="BodyStrong"
            Foreground="{ui:ThemeResource TextFillColorPrimaryBrush}"
            Text="Highlighter options"
            TextOptions.TextFormattingMode="Ideal"
            TextOptions.TextRenderingMode="Auto"
            TextWrapping="WrapWithOverflow" />
        <StackPanel Orientation="Horizontal">
            <ui:Card
                Width="300"
                Margin="0,10,10,10"
                Padding="20,20,20,20"
                HorizontalAlignment="Left">
                <StackPanel Margin="0,0,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Radius" />
                        <Slider
                            x:Name="sldSpotlightRadius"
                            Grid.Column="1"
                            Width="200"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            AutoToolTipPlacement="TopLeft"
                            Maximum="100"
                            Minimum="0"
                            Value="{Binding ViewModel.SpotlightRadius, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Opacity" />
                        <Slider
                            x:Name="sldSpotlightOpacity"
                            Grid.Column="1"
                            Width="200"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            AutoToolTipPlacement="TopLeft"
                            Maximum="100"
                            Minimum="0"
                            Value="{Binding ViewModel.SpotlightOpacity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                </StackPanel>
            </ui:Card>
            <ui:Card
                Width="300"
                Margin="0,10,10,10"
                Padding="20,20,20,20"
                HorizontalAlignment="Left">
                <StackPanel Margin="0,0,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Radius" />
                        <Slider
                            x:Name="sldSpotlightRadiuss"
                            Grid.Column="1"
                            Width="200"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            AutoToolTipPlacement="TopLeft"
                            Maximum="100"
                            Minimum="0"
                            Value="{Binding ViewModel.SpotlightRadius, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                    <Grid Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Opacity" />
                        <Slider
                            x:Name="sldSpotlightOpacitys"
                            Grid.Column="1"
                            Width="200"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            AutoToolTipPlacement="TopLeft"
                            Maximum="100"
                            Minimum="0"
                            Value="{Binding ViewModel.SpotlightOpacity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                </StackPanel>
            </ui:Card>
        </StackPanel>
        <controls:ExpandableSettingsControl
            ContentPadding="50,0,30,0"
            Header="Highlighter shadow"
            HeaderContentPadding="0,0,40,0"
            Icon="{ui:SymbolIcon AlignSpaceEvenlyVertical20}"
            IsExpanded="True">
            <controls:ExpandableSettingsControl.ExpanderAction>
                <ui:ToggleSwitch OffContent="Off" OnContent="On" />
            </controls:ExpandableSettingsControl.ExpanderAction>
            <StackPanel>
                <ui:CardControl Margin="0,0,0,12" Icon="{ui:SymbolIcon AlignSpaceEvenlyVertical20}">
                    <ui:CardControl.Header>
                        <ui:TextBlock
                            Grid.Row="0"
                            FontTypography="Body"
                            Text="Navigation style" />
                    </ui:CardControl.Header>
                    <ComboBox Grid.Column="1" MinWidth="200">
                        <ComboBoxItem Content="Left compact" />
                        <ComboBoxItem Content="Fluent" />
                        <ComboBoxItem Content="Top" />
                        <ComboBoxItem Content="Bottom" />
                    </ComboBox>
                </ui:CardControl>
                <ui:TextBlock
                    Grid.Row="0"
                    FontTypography="Body"
                    Text="Navigation style" />
            </StackPanel>
        </controls:ExpandableSettingsControl>
        <ui:CardControl Margin="0,0,0,12" Icon="{ui:SymbolIcon AlignSpaceEvenlyVertical20}">
            <ui:CardControl.Header>
                <ui:TextBlock
                    Grid.Row="0"
                    FontTypography="Body"
                    Text="Navigation style" />
            </ui:CardControl.Header>
            <ComboBox Grid.Column="1" MinWidth="200">
                <ComboBoxItem Content="Left compact" />
                <ComboBoxItem Content="Fluent" />
                <ComboBoxItem Content="Top" />
                <ComboBoxItem Content="Bottom" />
            </ComboBox>
        </ui:CardControl>
    </StackPanel>
</Page>

