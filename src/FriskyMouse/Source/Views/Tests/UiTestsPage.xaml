<Page
    x:Class="FriskyMouse.Views.Pages.UiTestsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker"
    xmlns:controls="clr-namespace:FriskyMouse.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:FriskyMouse.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Mouse Spotlight Page"
    Width="865"
    Height="560"
    d:DataContext="{d:DesignInstance local:UiTestsPage,
                                     IsDesignTimeCreatable=False}"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ColorPicker;component/Styles/DefaultColorPickerStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>

    </Page.Resources>

    <Grid Margin="{StaticResource MainContainerMargin}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="71*" />
            <ColumnDefinition Width="20*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <TextBlock
                FontSize="{StaticResource PageHeaderFontSize}"
                FontWeight="Normal"
                Text="Mouse Highlighter Settings" />
            <!--<ui:Button Grid.Column="0"
               Command="{Binding ViewModel.CounterIncrementCommand, Mode=OneWay}"
               Content="Click me!"
               Icon="Fluent24" />-->
            <Separator Margin="0,30,0,20" />
        </Grid>

        <StackPanel
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Orientation="Vertical">
            <StackPanel Orientation="Vertical">
                <controls:ColorPickerControl
                    Description="Select the color of the spotlight's outline"
                    Header="Outline color"
                    Icon="{ui:SymbolIcon FlashSettings24}" />
                <controls:SettingsCard Description="This is a description of what the user  can do with this settings. This is a description of what the user  can do with this settings." Header="This is a header">
                    <ui:ToggleSwitch OffContent="Disabled" OnContent="Enabled" />
                </controls:SettingsCard>

                <controls:SettingsCard Header="Description" Icon="{ui:SymbolIcon FlashSettings24}">
                    <ui:ToggleSwitch OffContent="Disabled" OnContent="Enabled" />
                </controls:SettingsCard>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:CardExpander
                Grid.Column="0"
                HorizontalAlignment="Stretch"
                Icon="{ui:SymbolIcon PlaySettings20}">
                <ui:CardExpander.Header>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <ui:TextBlock
                            Grid.Row="0"
                            Grid.Column="0"
                            FontSize="16"
                            FontTypography="Body"
                            Text="This is a header text" />
                        <ui:TextBlock
                            Grid.Row="1"
                            Grid.Column="0"
                            FontSize="12"
                            Foreground="{ui:ThemeResource TextFillColorSecondaryBrush}"
                            Text="This is a description text" />
                        <ui:ToggleSwitch
                            Grid.Row="0"
                            Grid.RowSpan="2"
                            Grid.Column="1"
                            Margin="0,0,16,0"
                            OffContent="Off"
                            OnContent="On" />
                    </Grid>
                </ui:CardExpander.Header>
                <StackPanel Margin="24,0.5,24,0">
                    <controls:SettingsCard
                        Description="Testing the SettingsCard "
                        Header="Testing the SettingsCard "
                        Icon="{ui:SymbolIcon FlashSettings24}">
                        <controls:ColorPickerControl />
                    </controls:SettingsCard>
                    <controls:SettingsCard
                        Description="Testing the SettingsCard "
                        Header="Testing the SettingsCard "
                        Icon="{ui:SymbolIcon FlashSettings24}">
                        <ui:DropDownButton Content="Hello" Icon="{ui:SymbolIcon Fluent24}">
                            <ui:DropDownButton.Flyout>
                                <ContextMenu>
                                    <MenuItem Header="Add" />
                                    <MenuItem Header="Remove" />
                                    <MenuItem Header="Send" />
                                    <MenuItem Header="Hello" />
                                </ContextMenu>
                            </ui:DropDownButton.Flyout>
                        </ui:DropDownButton>
                    </controls:SettingsCard>
                </StackPanel>
            </ui:CardExpander>
            <!--  TODO: ExpandDirection  -->
        </Grid>

        <!-- <ui:CardExpander
            Grid.Row="2"
            Header="Opacity"
            Icon="{ui:FontIcon '&#x1F308;'}">
            <StackPanel Opacity="0.9" Orientation="Vertical">
                <controls:SettingsCard Grid.Row="2">
                    <Slider Width="200" AutoToolTipPlacement="TopLeft" />
                </controls:SettingsCard>
                <controls:SwitchCard
                    Grid.Row="2"
                    IsToggleSwitchEnabled="{Binding ViewModel.IsMouseSpotlightEnabled, Mode=TwoWay}"
                    SwitchStatusText="{Binding ViewModel.SwitchStatusText}" />
            </StackPanel>
        </ui:CardExpander>-->

        <!--  TODO: Change the font family of the card header  -->
        <!--<controls:SettingsCard
            Grid.Row="3"
            Description="Testing the SettingsCard "
            Header="Testing the SettingsCard "
            Icon="{ui:SymbolIcon FlashSettings24}">
            <ui:ToggleSwitch />
        </controls:SettingsCard>-->

        <!--<ui:CardControl
            Grid.Row="4"
            Margin="20,1,30,0"
            Icon="{ui:SymbolIcon DocumentEdit20,
                                 FontSize=43,
                                 Filled=False}">
            <ui:CardControl.Header>
                <ui:TextBlock
                    FontSize="16"
                    FontWeight="Normal"
                    Text="This is a header" />
            </ui:CardControl.Header>
            <colorpicker:PortableColorPicker
                Width="48"
                Height="40"
                AllowDrop="True"
                HintColor="Yellow"
                SelectedColor="{Binding ViewModel.SelectedColor, Mode=TwoWay}"
                ShowAlpha="False"
                ShowFractionalPart="False"
                SmallChange="5"
                UseHintColor="True" />
        </ui:CardControl>-->
        <!--<StackPanel Grid.Row="4">

            <ui:CardControl
                Grid.Row="5"
                Padding="15,0,24,0"
                Icon="{ui:SymbolIcon FlashSettings24}">
                <ui:CardControl.Header>
                    <StackPanel>
                        <ui:TextBlock
                            Margin="0"
                            FontTypography="BodyStrong"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Text="This the header text"
                            TextWrapping="WrapWithOverflow" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="This is a description text."
                            TextWrapping="WrapWithOverflow" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:ToggleSwitch
                    HorizontalContentAlignment="Left"
                    IsEnabled="True"
                    OffContent="Off"
                    OnContent="On" />
            </ui:CardControl>
            <ui:CardControl
                Grid.Row="5"
                Margin="{StaticResource CardControlPadding}"
                Padding="20,10,20,10"
                Icon="{ui:SymbolIcon FlashSettings24}">
                <ui:CardControl.Header>
                    <StackPanel>
                        <ui:TextBlock
                            Margin="0"
                            FontTypography="BodyStrong"
                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                            Text="This the header text"
                            TextWrapping="WrapWithOverflow" />
                        <ui:TextBlock
                            Appearance="Secondary"
                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                            Text="This is a description text."
                            TextWrapping="WrapWithOverflow" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:ToggleSwitch
                    HorizontalContentAlignment="Left"
                    IsEnabled="True"
                    OffContent="Off"
                    OnContent="On" />
            </ui:CardControl>
        </StackPanel>-->
        <!--<ui:CardControl
            Grid.Row="5"
            Margin="{StaticResource CardControlPadding}"
            Padding="30,10,20,10"
            Icon="{ui:SymbolIcon FlashSettings24}">
            <ui:CardControl.Header>
                <StackPanel>
                    <ui:TextBlock
                        Margin="0"
                        FontTypography="BodyStrong"
                        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                        Text="This the header text"
                        TextWrapping="WrapWithOverflow" />
                    <ui:TextBlock
                        Appearance="Secondary"
                        Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                        Text="This is a description text."
                        TextWrapping="WrapWithOverflow" />
                </StackPanel>
            </ui:CardControl.Header>
            <ui:ToggleSwitch
                HorizontalContentAlignment="Left"
                IsEnabled="True"
                OffContent="Off"
                OnContent="On" />
        </ui:CardControl>
        <ui:CardAction Grid.Row="6" Icon="{ui:SymbolIcon DocumentEdit20, FontSize=43, Filled=False}">
            <StackPanel>
                <ui:TextBlock
                    Margin="0"
                    FontTypography="BodyStrong"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    Text="This the header text"
                    TextWrapping="WrapWithOverflow" />
                <ui:TextBlock
                    Appearance="Secondary"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    Text="This is a description text."
                    TextWrapping="WrapWithOverflow" />
            </StackPanel>
        </ui:CardAction>-->
        <!--<StackPanel Grid.Row="1" Margin="0,20,0,0">
            <ui:ToggleSwitch
                x:Name="tglSpotlight"
                Grid.Column="0"
                Content="Enable/Disable Spotlight"
                IsChecked="{Binding ViewModel.IsMouseSpotlightEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <ui:Card Grid.Row="2" Margin="0,20,0,0">
            <Grid>
                <TextBlock
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Text="Test" />
                <ui:ToggleSwitch
                    x:Name="tglOi"
                    HorizontalAlignment="Right"
                    Content="Enable/Disable Spotlight"
                    IsChecked="{Binding ViewModel.IsMouseSpotlightEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
        </ui:Card>-->

        <!--<ui:Card Grid.Row="1" Margin="0,0.5,0,0">
            <Grid>
                <TextBlock
                    Padding="10"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Text="Test" />
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <ui:TextBlock
                        Margin="0,0,10,0"
                        VerticalAlignment="Center"
                        Text="{Binding ViewModel.SwitchStatusText}" />
                    <ui:ToggleSwitch
                        x:Name="tglOsi"
                        Content=""
                        IsChecked="{Binding ViewModel.IsMouseSpotlightEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>

            </Grid>
        </ui:Card>-->
        <!--<ui:CardExpander Grid.Row="3" Header="Oiii Click Me!" />-->
        <!--<epco:PageControlDocumentation Grid.Row="2" />
        <controls:SliderCard Grid.Row="2" Background="red" />-->

        <!--
        <controls:SwitchCard
            Grid.Row="3"
            IsToggleSwitchEnabled="{Binding ViewModel.IsMouseSpotlightEnabled, Mode=TwoWay}"
            SwitchStatusText="{Binding ViewModel.SwitchStatusText}" />-->
        <!--<ui:Card Grid.Row="4" Footer="Oiiiiiiii">
            <Slider Maximum="100" Minimum="0" />
        </ui:Card>-->
        <!--<ui:Card Grid.Row="3" Footer="This is a footer">
            <ui:TextBlock Text="Test" />
        </ui:Card>-->
        <!--
         Displays a glyph
        <TextBlock
            Grid.Row="3"
            FontFamily="Segoe UI Symbol"
            Foreground="Wheat"
            Text="&#xE001;" />-->
    </Grid>
</Page>

